---
description: RenderDoc/RDC GPU Debug project rule
alwaysApply: true
---

# Cursor Project Rules

## Identity

- 项目：RenderDoc/RDC GPU Debug Framework
- 平台：Cursor
- 目标：使用 RenderDoc/RDC platform tools 调试 GPU 渲染问题

## Required Reads

1. `AGENTS.md`
2. `common/AGENT_CORE.md`
3. `common/config/platform_adapter.json`
4. `common/docs/platform-capability-model.md`

## Hard Rules

- 平台启动后默认保持普通对话态；只有用户手动召唤 `rdc-debugger`，才进入 RenderDoc/RDC GPU Debug 调试框架
- 除 `rdc-debugger` 之外，其他 specialist 默认都是 internal/debug-only，只能由 `rdc-debugger` 在框架内分派
- 进入平台真相相关工作前，必须先校验 `paths.tools_source_root` 与 `validation.required_paths`，并将 `tools/` 视为 package-local source payload
- 用户未提供至少一份可导入的 `.rdc` 前，必须以 `BLOCKED_MISSING_CAPTURE` 直接阻断
- 当前正式支持能力是 local-first 调试、native agents/skills、wrapper-dispatched pseudo-hooks 与 `run_compliance.yaml` 合规裁决
- experimental remote / live bridge / rehydrate 不属于当前正式默认路径；若任务依赖它，必须显式回报限制

## Workspace

- 运行区固定为 `workspace/`，按 `cases/<case_id>/runs/<run_id>/` 组织
- 并行 live case 必须使用独立 `context/daemon`；不得把多个 live case 塞进同一 context
- 结案时仍以 `artifacts/run_compliance.yaml(status=passed)` 作为统一合规裁决
