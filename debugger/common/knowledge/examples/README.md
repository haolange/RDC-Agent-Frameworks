# RenderDoc/RDC GPU Debug · Knowledge Examples

本目录只存放示例、教学样例与审计夹具。

规则：

- 这里的文件不能被当成 live library session artifacts。
- hooks、validator 与 run compliance audit 不得把本目录中的示例当成当前 run 的真实结案证据。
- 真实 session 级 gate artifacts 仍固定写入 `../library/sessions/<session_id>/`。

